@model Digikala.DataAccessLayer.Entities.Identity.Permission
<form method="post" asp-action="EditPermission">
     <input type="hidden" asp-for="Id"/>
     <div class="modal-body">
          <div class="form-group">
               <label for="recipient-name" class="col-form-label">عنوان :</label>
               <input required asp-for="Name" type="text" class="form-control" id="recipient-name">
          </div>
          <div class="form-group">
               <label for="recipient-name" class="col-form-label" asp-for="ParentId"></label>
               <select asp-for="ParentId" asp-items="@ViewData["Permissions"] as SelectList" id="permissions" class="form-control"></select>
          </div>
          <div class="form-group">
               <label class="col-3 col-form-label">سر گروه میباشد</label>
               <div class="col-3">
                    <span class="switch switch-outline switch-icon switch-success">
                         <label>
                              <input type="checkbox" name="isParent" id="checkbox"/>
                              <span></span>
                         </label>
                    </span>
               </div>
          </div>
     </div>
     <div class="modal-footer">
          <a href="#" class="btn btn-warning btn-shadow-hover font-weight-bold mr-2" data-dismiss="modal">بستن</a>
          <button type="submit" class="btn btn-success btn-shadow-hover font-weight-bold mr-2">ثبت</button>
     </div>
</form>
<script>
     $(document).ready(function () {
          var input = document.getElementById("permissions");
          var checkbox = document.getElementById('checkbox');
          if (input.value && input.value == 0) {
               input.setAttribute("disabled", "disabled");
               checkbox.setAttribute("checked","checked");
          }
     });

     var checkbox = document.getElementById('checkbox');
     var input = document.getElementById("permissions");
     checkbox.addEventListener("click", function () {
          input.toggleAttribute("disabled");
     });
</script>